# React 19 / zod 4 / Next 16 are ahead of several libraries' declared peer
# ranges (react-hook-form, @hookform/resolvers, drizzle-zod). Relax peer
# resolution so local, CI, and the Render/Vercel deploys all install the same
# way. Lockfile still pins exact versions.
legacy-peer-deps=true
